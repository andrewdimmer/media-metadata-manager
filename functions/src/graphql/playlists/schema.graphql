type Playlist {
  id: ID!
  type: TagType!
  name: String!
  description: String!
  filter: String!
}

input CreatePlaylistInput {
  name: String!
  description: String!
  filter: String!
}

input UpdatePlaylistInput {
  id: ID!
  name: String
  description: String
}

input DeletePlaylistInput {
  id: ID!
}
