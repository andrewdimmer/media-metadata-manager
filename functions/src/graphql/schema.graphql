schema {
  query: Query
  mutation: Mutation
}

type Query {
  tags: [Tag!]!
  tag(id: ID!): Tag!
}

type Mutation {
  createTag(input: CreateTagInput): Tag!
  deleteTag(input: DeleteTagInput): Tag!
}
